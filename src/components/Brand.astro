---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a class="brand" href={href}>
  {label}<span class="terminal-prompt" aria-hidden="true">&gt;<span class="terminal-cursor">_</span></span>
</a>
