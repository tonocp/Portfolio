---
interface Capability {
  title: string;
  description: string;
}

interface Props {
  eyebrow: string;
  title: string;
  items: readonly Capability[];
  stack: readonly string[];
  stackAriaLabel: string;
}

const { eyebrow, title, items, stack, stackAriaLabel } = Astro.props;
---

<section class="section shell capabilities">
  <div class="section-heading">
    <p class="eyebrow">{eyebrow}</p>
    <h2>{title}</h2>
  </div>

  <div class="capability-grid">
    {
      items.map((item) => (
        <article class="capability-card">
          <h3>{item.title}</h3>
          <p>{item.description}</p>
        </article>
      ))
    }
  </div>

  <ul class="stack-cloud" aria-label={stackAriaLabel}>
    {stack.map((item) => <li>{item}</li>)}
  </ul>
</section>
